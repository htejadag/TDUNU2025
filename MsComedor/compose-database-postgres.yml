version: "1"

services:
  postgres:
    image: postgres:latest
    container_name: db-postgres
    restart: always
    environment:
    - POSTGRES_PASSWORD=Unu#2025
    ports:
    - 5433:5432
    networks:
    - td2025
    
networks:
  td2025:
    name: ms-unu

