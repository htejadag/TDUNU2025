<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

  <!-- Para usar propiedades del application.properties -->
  <springProperty scope="context" name="logPath" source="app.log.path" />

  <!-- Nivel por defecto -->
  <root level="INFO">
    <appender-ref ref="CONSOLE" />
    <appender-ref ref="FILE_APP" />
    <appender-ref ref="FILE_ERROR" />
  </root>

  <!-- ====== Consola ====== -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ====== Log diario general ====== -->
  <appender name="FILE_APP"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>MsPlanEstudios/logs/app.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <!-- Genera un archivo por día -->
      <fileNamePattern>MsPlanEstudios/logs/app-%d{yyyy-MM-dd}.log.gz</fileNamePattern>

      <!-- Mantener 30 días de logs -->
      <maxHistory>30</maxHistory>

      <!-- Tamaño máximo antes de rotar -->
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>

    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- ====== Log exclusivo para errores ====== -->
  <appender name="FILE_ERROR"
    class="ch.qos.logback.core.rolling.RollingFileAppender">

    <file>logs/error.log</file>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <fileNamePattern>MsPlanEstudios/logs/error-%d{yyyy-MM-dd}.log.gz</fileNamePattern>
      <maxHistory>30</maxHistory>
    </rollingPolicy>

    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
    </encoder>

    <!-- Solo errores -->
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
      <level>ERROR</level>
      <onMatch>ACCEPT</onMatch>
      <onMismatch>DENY</onMismatch>
    </filter>
  </appender>

</configuration>
